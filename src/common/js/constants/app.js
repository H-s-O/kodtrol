export const APP_NAME = 'Kodtrol';
export const PROJECT_FILE_EXTENSION = 'kodtrol';

export const SCRIPT_TEMPLATE =
`// function start(devices) {
//  
// }

// function leadInFrame(devices, { blockPercent, inTime, outTime, currentTime }, triggers, curves) {
//  
// }

function frame(devices, { blockPercent, inTime, outTime, currentTime }, triggers, curves) {
  
}

// function leadOutFrame(devices, { blockPercent, inTime, outTime, currentTime }, triggers, curves) {
//  
// }

// function beat(devices, { localBeat, globalBeat }) {
//  
// }

// function input(devices, inputType, inputData) {
//  
// }`;
